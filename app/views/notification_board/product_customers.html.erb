<%# app/views/notification_board/product_customers.html.erb — rendered layout:false, fetched via JS %>
<% if @rows.blank? %>
  <div class="text-muted small py-2">目前沒有符合條件的客人（可能都已回購或資料已更新）</div>
<% else %>
  <div class="table-responsive">
    <table class="table table-sm table-hover align-middle mb-0" style="font-size:0.8rem;">
      <thead class="table-light">
        <tr>
          <th>姓名</th><th>卡別</th><th>Email</th><th>LINE ID</th><th>名單狀態</th>
        </tr>
      </thead>
      <tbody>
        <% @rows.each do |r| %>
          <tr class="<%= new_entrant_row_class(r[:customer_id], @previous_customer_ids) %>">
            <td>
              <% if r[:customer_id] %>
                <%= link_to (r[:full_name].presence || "（無名）"), customer_path(r[:customer_id]), target: "_blank", class: "fw-semibold text-decoration-none" %>
              <% else %>
                <span class="text-muted">（查無會員資料）</span>
              <% end %>
            </td>
            <td><span class="text-muted small"><%= r[:membership_level].presence || "一般" %></span></td>
            <td><span class="text-muted small"><%= r[:email] %></span></td>
            <td><span class="text-muted small"><%= r[:line_id].presence || "—" %></span></td>
            <td><%= customer_roster_badge(r[:customer_id], @previous_customer_ids) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>

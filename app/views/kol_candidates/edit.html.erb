<% content_for :page_title, "編輯 #{@candidate.name}" %>

<div class="row g-3 mb-3">
  <div class="col-12">
    <h4 class="fw-bold mb-0">編輯：<%= @candidate.name %></h4>
  </div>
</div>

<div class="card border-0 shadow-sm">
  <div class="card-body">
    <%= render "form" %>
  </div>
</div>
